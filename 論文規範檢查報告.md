# 論文規範檢查報告(最終版)

依 MIRLAB 學位論文規範,對 PD-Master-Thesis(英文論文,含中英文摘要)做完整檢查與修正。以下為逐條結果與已完成的修改。

## 已修正 (完成)

### 規則 6:章節簡介
原本第 1、3、4、6 章從章標題直接跳到第一個 section,現已全部補上實質簡介段落(並補齊被引用的 section label):
- `chapter01.tex` — Introduction 簡介
- `chapter03.tex` — Proposed Methods 簡介
- `chapter04.tex` — Dataset and Experiment Setting 簡介
- `chapter06.tex` — Conclusions and Future Work 簡介

(第 2、5 章原本即有簡介,符合。)

### 規則 12:圖表不得出現在章節最上方
- `chapter03.tex` — `Overall Pipeline` 原本一開頭就是 pipeline 圖,已把說明文字移到圖前。

### 規則 5:摘要只能有一段
- 中文 `abstract` 與英文 `abstract*` 原各為兩段,已各自合併為單一段落。

### 規則 4:專有名詞縮寫
- 移除重複定義:`chapter04.tex` 的 NTUH、LOOCV 第二次出現改用縮寫。
- 補上首次全名:ANOVA、AUROC、SSD、SMOTE、RGB、CPU、GPU(ch2)、RMS(ch3)、RMSE(ch5)、Analysis of Variance(ch1)。
- 已正確定義且無需改:PD、MDS-UPDRS、LOOCV、NTUH、ICC、SGC-LL(緊接內文定義)、CMC/MCP/PIP/DIP/IP(附錄 A 表下方有縮寫對照)。

### 3.4.1 數學式簡化(依實際程式碼核對)
- 對照 `xgb_exp/xgb_loocv_eval.py` 的 `build_graph_features_for_patient`,論文與程式碼完全一致(203 = 168 + 35;1,764 + 203 = 1,967)。
- 已將 3.4.1 的五條 equation 全部改為文字敘述(移除的 3 個 equation label 未被他處引用,不影響交互參照)。

## 已符合(檢查通過)

- **章節結構**:緒論、文獻探討、研究方法、資料集與實驗設定(第 4 章)、實驗結果、結論與未來展望齊全。自動檢查報「缺 experimental-setup」為誤判(第 4 章已涵蓋 Roadmap/Setting)。
- **頁數**:編譯後 77 頁 ≥ 50。
- **Section 標題大小寫**:全部首字大寫/title case。
- **圖標題在下、表標題在上**:全部正確。自動檢查報的 10 筆「figure caption 位置錯」皆為 `subfigure` 子標題(正確在子圖下方)之誤判。
- **借用他人圖片**:全書圖檔皆為作者自產結果圖/流程圖,無借用外部架構圖,故無需 caption 引用。
- **列表縮排**:使用正規 `itemize`/`enumerate`。
- **括號/`\cite` 前空格**:未發現違規。
- **中文內文夾雜英文大小寫**:摘要等中文段落中的英文均為專有名詞/縮寫,正確。

## 需人工/後續處理

- **規則 8 圖內文字大小**:matplotlib 產生的 ROC/scatter/loss 圖,字級一般沒問題,但仍需你目視確認圖內文字不大於內文。
- **規則 2 英文潤稿**:已做針對性修正,但全篇 LLM 潤稿(並統一英/美式拼寫,如 optimised/normalized 混用)屬大範圍改寫,建議另跑一次。
- **重新編譯**:此環境缺中文 ctex 套件,無法產生新 PDF,請在本機執行 `latexmk -xelatex main.tex` 確認排版無誤。

## 自動檢查器殘留(全部為誤判,無需處理)
- figure-caption-position ×10:subfigure 子標題
- abstract-multiple-paragraphs:實為中文一段 + 英文一段
- missing-chapter experimental-setup:第 4 章語意相符
